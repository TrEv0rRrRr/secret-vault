const LoginReminder = () => {
  return (
    <p className="text-white">
      To save new secrets you have to create an account. <br />
      Already have one? Just log in.
    </p>
  );
};

export default LoginReminder;
